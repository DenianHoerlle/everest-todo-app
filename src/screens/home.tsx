import { Form, Header, TodoList } from "components";

const HomeScreen = () => {
  return (
    <div className="mt-20">
      <Header />
      <Form />
      <TodoList />
    </div>
  );
};

export default HomeScreen;
