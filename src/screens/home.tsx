import { Form, Header, TodoList } from "components";

const HomeScreen = () => {
  return (
    <div>
      <Header />
      <Form />
      <TodoList />
    </div>
  );
};

export default HomeScreen;
